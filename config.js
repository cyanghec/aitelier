// AItelier — backend API configuration
// After deploying the backend to Render, paste the URL here and push.
// Example: 'https://aitelier-api.onrender.com'
// Leave empty to fall back to http://localhost:8000 (local dev).
window.AITELIER_API = '';
